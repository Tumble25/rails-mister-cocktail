<h2><%= @cocktail.name %></h2>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "add the ingredient & dose", class: "btn btn-primary" %>
<% end %>
